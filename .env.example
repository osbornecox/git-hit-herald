# LLM Provider (choose one)
OPENAI_API_KEY=sk-...
# or
ANTHROPIC_API_KEY=sk-ant-...

# LLM Provider selection: "openai" (default) or "anthropic"
LLM_PROVIDER=openai

# Optional: for fetching Replicate models
REPLICATE_API_TOKEN=r8_...

# Optional: for higher rate limits when fetching GitHub README
GITHUB_TOKEN=ghp_...

# Telegram (optional - for Telegram digest delivery)
TELEGRAM_BOT_TOKEN=...
TELEGRAM_CHAT_ID=...

# Slack (optional - for Slack digest delivery)
# Get webhook URL: https://api.slack.com/apps → Create App → Incoming Webhooks
SLACK_WEBHOOK_URL=https://hooks.slack.com/services/...
